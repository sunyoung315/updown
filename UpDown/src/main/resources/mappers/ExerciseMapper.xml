<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.updown.model.dao.ExerciseDao">
	<!-- 오늘의 운동 등록 -->
	<insert id="upload" parameterType="Exercise">
		INSERT INTO exercise (type, time, calorie, reg_date, user_id)
		VALUES (#{type}, #{time}, #{calorie}, date_format(now(), '%Y-%m-%d'), #{userId});
	</insert>
	
	<!-- 해당 일자에 등록된 운동 기록 리스트 가져오기 -->
	<select id="selectList" parameterType="String" resultType="Exercise">
		SELECT * 
		FROM exercise 
		WHERE reg_date=date_format(now(), '%Y-%m-%d') AND user_id=#{loginUserId};
	</select>
	
	<!-- 선택한 운동 기록 1개 가져오기 -->
	<select id="selectOne" parameterType="int" resultType="Exercise">
		SELECT *
		FROM exercise
		WHERE no = #{exerciseNo};
	</select>
	
	<!-- 선택한 운동 기록 수정 -->
	<update id="modify" parameterType="int">
		UPDATE exercise
		SET type = #{type}, time = #{time}, calorie = #{calorie}
		WHERE no = #{exerciseNo};
	</update>
	
	<!-- 선택한 운동 기록 삭제 -->
	<delete id="delete" parameterType="int">
		DELETE FROM exercise
		WHERE no = #{exerciseNo};
	</delete>
</mapper>